<!DOCTYPE html>
<html lang="en">


    <%- include("../partials/head") %>
<body>
<%- include("../partials/header") %>
<%- include("../partials/navigation") %>

    <img src="/images/site/small_check.jpg" alt="checknerboard pattern" class="bg-pattern">

    <main class="management-container">
        <h1><%= title %></h1>
        
        <% if (message && message.length) { %>
        <div class="notice"><%= message %></div>
        <% } %>

        <% if (errors && errors.length > 0) { %>
        <div class="error-list">
            <ul>
            <% errors.forEach(err => { %>
                <li><%= err.msg %></li>
            <% }) %>
            </ul>
        </div>
        <% } %>

        <section class="management-links">
            <h2>What would you like to do?</h2>

            <ul>
                <li>
                    <a href="/inv/new/classification">Add New Classification</a>
                </li>
                <li>
                    <a href="/inv/new/inventory">Add New Inventory Item</a>
                </li>
            </ul>

            <h2>Manage Inventory</h2>
            <p>Please select a classification from the list so you can see the items</p>

            <%- classificationList %>

            <table id="inventoryDisplay"></table>

            <noscript>JavaScript must be enable to use this page</noscript>

        </section>
    </main>
        <%- include("../partials/footer") %>

        <script src="/js/inventory.js" defer></script>
</body>
</html>